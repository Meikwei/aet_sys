// Code generated by https://github.com/zhufuyi/sponge
// Test_service_sysRole_methods is used to test the sysRole api
// Test_service_sysRole_benchmark is used to performance test the sysRole api

package service

import (
	"context"
	"encoding/json"
	"fmt"
	"testing"
	"time"

	"github.com/zhufuyi/sponge/pkg/grpc/benchmark"

	sysV1 "github.com/Meikwei/aet_sys/api/sys/v1"
	"github.com/Meikwei/aet_sys/configs"
	"github.com/Meikwei/aet_sys/internal/config"
)

// Test service sysRole api via grpc client
func Test_service_sysRole_methods(t *testing.T) {
	conn := getRPCClientConnForTest()
	cli := sysV1.NewSysRoleClient(conn)
	ctx, _ := context.WithTimeout(context.Background(), time.Second*30)

	tests := []struct {
		name    string
		fn      func() (interface{}, error)
		wantErr bool
	}{

		{
			name: "Create",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &sysV1.CreateSysRoleRequest{
					RoleName:   "", // 角色名称
					RoleDesc:   "", // 角色描述
					CreateUser: "", // 创建人
					UpdateUser: "", // 更新人
				}

				return cli.Create(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "DeleteByID",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &sysV1.DeleteSysRoleByIDRequest{
					Id: 0,
				}

				return cli.DeleteByID(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "DeleteByIDs",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &sysV1.DeleteSysRoleByIDsRequest{
					Ids: nil,
				}

				return cli.DeleteByIDs(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "UpdateByID",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &sysV1.UpdateSysRoleByIDRequest{
					Id:         0,  // 角色ID
					RoleName:   "", // 角色名称
					RoleDesc:   "", // 角色描述
					CreateUser: "", // 创建人
					UpdateUser: "", // 更新人
				}

				return cli.UpdateByID(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "GetByID",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &sysV1.GetSysRoleByIDRequest{
					Id: 0,
				}

				return cli.GetByID(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "GetByCondition",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &sysV1.GetSysRoleByConditionRequest{
					Conditions: nil,
				}

				return cli.GetByCondition(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "ListByIDs",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &sysV1.ListSysRoleByIDsRequest{
					Ids: nil,
				}

				return cli.ListByIDs(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "ListByLastID",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &sysV1.ListSysRoleByLastIDRequest{
					LastID: 0,  // last id
					Limit:  0,  // limit size per page
					Sort:   "", // sort by column name of table, default is -id, the - sign indicates descending order.
				}

				return cli.ListByLastID(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "List",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &sysV1.ListSysRoleRequest{
					Params: nil,
				}

				return cli.List(ctx, req)
			},
			wantErr: false,
		},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, err := tt.fn()
			if (err != nil) != tt.wantErr {
				t.Errorf("test '%s' error = %v, wantErr %v", tt.name, err, tt.wantErr)
				return
			}
			data, _ := json.MarshalIndent(got, "", "    ")
			fmt.Println(string(data))
		})
	}
}

// performance test service sysRole api, copy the report to
// the browser to view when the pressure test is finished.
func Test_service_sysRole_benchmark(t *testing.T) {
	err := config.Init(configs.Path("sys.yml"))
	if err != nil {
		panic(err)
	}
	if len(config.Get().GrpcClient) == 0 {
		t.Error("grpcClient is not set in sys.yml")
		return
	}
	host := fmt.Sprintf("%s:%d", config.Get().GrpcClient[0].Host, config.Get().GrpcClient[0].Port)
	protoFile := configs.Path("../api/sys/v1/sysRole.proto")
	// If third-party dependencies are missing during the press test,
	// copy them to the project's third_party directory.
	dependentProtoFilePath := []string{
		configs.Path("../third_party"), // third_party directory
		configs.Path(".."),             // Previous level of third_party
	}

	tests := []struct {
		name    string
		fn      func() error
		wantErr bool
	}{

		{
			name: "Create",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &sysV1.CreateSysRoleRequest{
					RoleName:   "", // 角色名称
					RoleDesc:   "", // 角色描述
					CreateUser: "", // 创建人
					UpdateUser: "", // 更新人
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "Create", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "DeleteByID",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &sysV1.DeleteSysRoleByIDRequest{
					Id: 0,
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "DeleteByID", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "DeleteByIDs",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &sysV1.DeleteSysRoleByIDsRequest{
					Ids: nil,
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "DeleteByIDs", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "UpdateByID",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &sysV1.UpdateSysRoleByIDRequest{
					Id:         0,  // 角色ID
					RoleName:   "", // 角色名称
					RoleDesc:   "", // 角色描述
					CreateUser: "", // 创建人
					UpdateUser: "", // 更新人
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "UpdateByID", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "GetByID",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &sysV1.GetSysRoleByIDRequest{
					Id: 0,
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "GetByID", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "GetByCondition",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &sysV1.GetSysRoleByConditionRequest{
					Conditions: nil,
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "GetByCondition", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "ListByIDs",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &sysV1.ListSysRoleByIDsRequest{
					Ids: nil,
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "ListByIDs", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "ListByLastID",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &sysV1.ListSysRoleByLastIDRequest{
					LastID: 0,  // last id
					Limit:  0,  // limit size per page
					Sort:   "", // sort by column name of table, default is -id, the - sign indicates descending order.
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "ListByLastID", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "List",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &sysV1.ListSysRoleRequest{
					Params: nil,
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "List", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			err := tt.fn()
			if (err != nil) != tt.wantErr {
				t.Errorf("test '%s' error = %v, wantErr %v", tt.name, err, tt.wantErr)
				return
			}
		})
	}
}
